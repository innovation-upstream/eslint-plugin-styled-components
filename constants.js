module.exports = {
  errors: {
    noMediaQuery: "Unexpected media query in styled-component, media queries should be placed in css modules to render all media queries during pre-render/ssr.",
    noExpression: "Unexpected expression in styled-component."
  }
}